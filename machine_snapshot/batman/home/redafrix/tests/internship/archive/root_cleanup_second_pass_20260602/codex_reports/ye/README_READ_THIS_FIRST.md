# Read This First

Start with:

`/home/redafrix/tests/internship/codex_reports/ye/FINAL_SIMVLA_UNCERTAINTY_FULL_REPORT.md`

Supporting files:

- `parsed_summary.json`: compact parsed metrics used by the final report.
- `bob_bundle/`: copied Bob Stage 8 reports/status/configs.
- `sam_bundle/`: copied Sam Stage 8 reports/status/configs.
- `sam_outputs_stage7/`: detailed target/flowtrace/model JSON files that were written outside the Stage 8 report directory.
- `bob_logs_tail/` and `sam_logs_tail/`: tail snapshots of job logs for error checking.

Important caveat: at the final snapshot, the main 20-episode LIBERO-PRO and 30-episode hard-LIBERO analyses were complete, but Bob still had the 50-episode LIBERO-PRO backup job marked RUNNING. The final report uses completed artifacts only.
