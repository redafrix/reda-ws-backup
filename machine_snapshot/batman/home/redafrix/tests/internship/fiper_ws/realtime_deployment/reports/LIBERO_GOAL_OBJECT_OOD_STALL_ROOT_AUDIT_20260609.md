# LIBERO GOAL OBJECT OOD STALL & ROOT AUDIT
**Date:** 2026-06-09
**Status:** READ-ONLY AUDIT COMPLETE

## 1. Stall and Exit Cause Analysis
- **Runner Used:** `/media/rootalkhatib/My Passport/reda_ws/fiper_ws/trash/h10_goal_object_ood_all_tasks_10ep_20260609/run_all.py`
- **Launch Command:** Could not be found in bash history, but executed via `python3 run_all.py`.
- **Logs:** The intended root does not use a central `logs/` directory for stdout/stderr, relying on the console output.
- **Tracebacks/OOM:** No `Traceback` or `OOM` strings were found in the `runs/` directory. System `journalctl` showed no memory errors.
- **Task Scope:** `run_all.py` iterates over `TASKS = range(18)`, meaning it was intended to run all tasks, not just Task 0.
- **Cause of Exit:** The final recorded action was the completion of `risk_topk8` Task 0 Seed 8 at **13:59:49+0200**. Exactly one second later, at **13:59:50**, `journalctl` recorded an SSH disconnection for the user `rootalkhatib`. Because the process was likely run directly in the SSH session (or in a tmux session that was killed upon disconnect), the SSH drop sent a SIGHUP signal, instantly killing the Python supervisor before Seed 9 could begin.

## 2. Intended Root Inspection
**Path:** `/media/rootalkhatib/My Passport/reda_ws/fiper_ws/trash/h10_goal_object_ood_all_tasks_10ep_20260609`
- **Configs Count:** 56 configs generated.
- **Expected Jobs Count:** 54 (18 tasks × 3 models).
- **Actual Runs Completed:** Only `task0/` exists in `runs/`.
- **Task 0 Row Counts:**
  - `original_simvla`: 10 episodes (Seeds 0-9).
  - `modified_simvla`: 10 episodes (Seeds 0-9).
  - `modified_h10_risk_topk8`: 9 episodes (Seeds 0-8). Seed 9 is missing.
- **Task 1 Status:** Configs for Task 1 exist (e.g., `task1_modified_simvla.json`), but no run directories or logs exist.
- **Scope:** `run_all.py` covers `TASKS = range(18)`.

## 3. Root Comparison
| Metric | Intended Root (`.../h10_goal_object_ood_all_tasks_10ep_20260609`) | Suspicious Root (`.../libero_goal_object_ood_full_sweep_20260609`) |
|---|---|---|
| **Creation Time** | Jun 9 13:54 | Jun 9 13:51 |
| **Purpose** | 10-episode production OOD sweep. | Appears to be an aborted 30-episode sweep attempt. |
| **Episode Count** | 10 (`range(10)`) | 30 (`range(30)`) |
| **Configs Generated** | 56 | Contains 30-ep specific configs (`online_jobs.json`). |
| **Supervisor Script** | `run_all.py` (Simple `range(18)` loop calling matrix runner). | `run_production_sweep_ood.py` (Complex logic, smoke tests). |
| **Production Data** | Contains complete data for Task 0. | Contains 1 episode of Task 0 before aborting. |
| **Paths Used** | Canonical: `/tmp/ood_ckpt60000`, `/tmp/ood_smolvlm_cache` | Deprecated: `/tmp/ckpt-60000-tmp` |

*Conclusion:* The suspicious root is an older (by 3 minutes), 30-episode setup using deprecated paths. It should be ignored. The intended root is canonical and holds the valid Task 0 data.

## 4. Task 0 Output Verification (Intended Root)
- **Suite:** Confirmed as `libero_goal_object_ood` in all JSONL files.
- **Policies:** Confirmed usage of `simvla_only` and `risk_topk8`.
- **Seeds:** Correctly sequenced (0-9 for simvla, 0-8 for risk). No duplicates.
- **Errors:** `error_message` strings are empty across all summaries.
- **NaNs/Anomalies:** No `NaN` or `null` values found in recorded risk scores. Data is completely clean and valid.

## 5. Risk_TopK8 Threshold Audit
- **Config Values:** The generated config `task0_modified_h10_risk_topk8.json` completely omits the `selection_controls` dictionary.
- **Runtime Defaults:** Because `selection_controls` was omitted in `run_all.py`, the runner (`src/run_policy_matrix.py`) fell back to its hardcoded default values.
- **Values Used:** The `run_manifest.json` confirms that the runtime defaults were applied. Instead of the aggressive `0.3` threshold, the script defaulted to `q95` (0.6155413389205933). Additionally, the default margins of `0.10` (min) and `0.15` (strong) were used instead of the intended aggressive margins of `0.02` and `0.05`.
- **Conclusion:** The `q95` default was accidentally used. As a result, the `risk_topk8` results collected for Task 0 are invalid for the intended aggressive test. The configs generated by `run_all.py` must be patched before the sweep is restarted so that the aggressive values are explicitly defined.

---
## SUMMARY FLAGS
AUDIT_READ_ONLY = YES
ANYTHING_RESTARTED = NO
INTENDED_ROOT_CONFIRMED = YES
SUSPICIOUS_SECOND_ROOT_USED = NO
TASK0_OUTPUT_CLEAN = YES
TASK1_CONFIGS_EXIST = YES
RUN_ALL_HAS_TASKS_0_TO_17 = YES
EXIT_CAUSE_FOUND = YES
EXIT_CAUSE = SSH disconnect at 13:59:50 sent SIGHUP, killing the supervisor.
SAFE_TO_RESUME_FROM_TASK1 = NO (The risk_topk8 threshold config is wrong; Task 0 risk_topk8 needs re-running after fix).
RESUME_COMMAND_PROPOSED = tmux new-session -d -s ood_sweep "python3 /media/rootalkhatib/My\ Passport/reda_ws/fiper_ws/trash/h10_goal_object_ood_all_tasks_10ep_20260609/run_all.py" (Note: Needs a modified run_all.py or runner idempotency to skip already completed seeds).
RISK_TOPK8_THRESHOLD_CONFIG_PASS = NO
RISK_TOPK8_THRESHOLD_RUNTIME_PASS = NO
TASK0_RISK_TOPK8_USED_AGGRESSIVE_0_3 = NO
TASK0_RISK_TOPK8_VALID_FOR_INTENDED_TEST = NO
THRESHOLD_FIX_NEEDED_BEFORE_RESUME = YES