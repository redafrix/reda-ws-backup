The V2 model checkpoint may be created locally on Dean for evaluation, but `model.pt`, `last_training_state.pt`, optimizer states, and other weight-bearing binaries must not be committed to Git in this task. Commit only code, manifests, hashes, JSON/CSV/TSV/text evidence, and small score summaries if explicitly allowed.